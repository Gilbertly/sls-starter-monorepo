{
  "name": "monorepo",
  "private": true,
  "scripts": {
    "deploy:dev": "lerna run --parallel deploy --since master",
    "build:prod": "lerna run build:prod",
    "clean:build": "lerna clean --yes && lerna bootstrap"
  }
}
