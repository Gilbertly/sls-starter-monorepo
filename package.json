{
  "name": "monorepo",
  "private": true,
  "scripts": {
    "deploy:dev": "lerna run deploy --since origin/master",
    "build:prod": "lerna run build:prod",
    "clean:build": "lerna clean --yes && lerna bootstrap"
  }
}
