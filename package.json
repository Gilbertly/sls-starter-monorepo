{
  "name": "monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "deploy:dev": "lerna run deploy",
    "build:prod": "lerna run build:prod",
    "clean:build": "lerna clean --yes && lerna bootstrap"
  }
}
